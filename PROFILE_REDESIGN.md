# 👤 PROFILE & FAVORITES REDESIGN

## Дата создания: 22 декабря 2025
## Последнее обновление: 25 декабря 2025, 21:00
## Статус: ✅ Фазы 1-5, 7 Завершены, Фаза 8 Частично

---

## 🎯 СТАТУС: Фаза 8 (частично) - Cleanup и документация

**Последнее обновление:** 25 декабря 2025, 21:00

### ✅ Что сделано в текущей сессии (25 декабря):

**Сессия 1: Фаза 4 - Компоненты коллекций**

1. **Завершена Фаза 4: Компоненты коллекций**
   - ✅ Создана страница публичного доступа `/collections/[id]/[share-token]`
     - Любой пользователь (даже неавторизованный) может просматривать публичные коллекции
     - Read-only режим для гостей
     - Отображается автор коллекции
     - Кнопка "Скопировать в мои коллекции" для авторизованных пользователей

   - ✅ Создан компонент `ShareCollectionModal` с улучшенным UI
     - Красивая модалка для шаринга коллекций
     - Удобное копирование ссылки
     - Возможность сделать коллекцию публичной прямо из модалки
     - Кнопка "Открыть в новой вкладке"

   - ✅ Реализован функционал копирования коллекций
     - Пользователи могут скопировать чужую публичную коллекцию себе
     - Создается новая приватная коллекция со всеми элементами
     - Автоматический редирект на новую коллекцию

2. **Исправлены ошибки:**
   - ✅ Исправлена таблица новостей в CollectionDetailPage (`architecture_news` вместо `news_posts`)
   - ✅ Исправлено поле изображения для новостей (`featured_image_url` вместо `image_url`)

3. **Улучшен UX на странице избранного:**
   - ✅ Создан компонент `CollectionsBadgeDropdown`
   - ✅ Бейджи коллекций заменены на интерактивные кнопки с выпадающим списком
   - ✅ Теперь можно увидеть все коллекции, в которых находится элемент, не переходя на другие страницы

**Сессия 2: Фаза 5 - Навигация и UserDropdown**

1. **Полностью переработан UserDropdown с применением дизайн-системы:**
   - ✅ Применены CSS-переменные (bg-card, border-border, text-foreground, hover:bg-accent)
   - ✅ Использован `rounded-[var(--radius)]` везде
   - ✅ Использован шрифт `font-metrics` для счетчиков
   - ✅ Добавлена кольцевая граница вокруг аватара (ring-2 ring-border)
   - ✅ Улучшен hover эффект (group-hover:text-primary для иконок)

2. **Упрощена структура меню до 10 основных пунктов:**
   - ✅ Мой профиль
   - ✅ Редактировать профиль
   - ✅ Мои объекты (с счетчиком)
   - ✅ Мои маршруты (с счетчиком)
   - ✅ Мои отзывы (с счетчиком)
   - ✅ Мои блоги (с счетчиком)
   - ✅ Избранное (с счетчиком)
   - ✅ Коллекции (с счетчиком)
   - ✅ Настройки
   - ✅ Админ панель (для модераторов/админов, с счетчиком заявок)

3. **Добавлены визуальные разделители:**
   - ✅ Разделитель после основных действий (профиль, редактирование)
   - ✅ Разделитель после моего контента
   - ✅ Разделитель перед настройками

4. **Улучшена статистика:**
   - ✅ Фон с легким оттенком (bg-muted/30)
   - ✅ Использование font-metrics для чисел
   - ✅ Изменено "Обзоров" на "Отзывы" для консистентности

5. **Убраны дублирующиеся пункты:**
   - ✅ Удалены дублирующиеся ссылки на модерацию
   - ✅ Оставлена только одна ссылка "Админ панель" с счетчиком заявок

**Сессия 3: Фаза 7 - Админ панель с дизайн-системой**

1. **Полностью переработан AdminLayout с боковым меню:**
   - ✅ Создана sticky sidebar с навигацией (w-72)
   - ✅ Применены CSS-переменные (bg-card, border-border, text-foreground)
   - ✅ Использован `rounded-[var(--radius)]` для всех элементов
   - ✅ Добавлена active state подсветка (bg-primary text-primary-foreground)
   - ✅ Использован `font-heading` для заголовков

2. **Структурированная навигация с группировкой:**
   - ✅ Группа без заголовка: "На сайт", "Dashboard"
   - ✅ Группа "Управление": "Модерация", "Пользователи"
   - ✅ Группа "Контент": "Новости", "Подкасты", "Автогенерация"

3. **User info в footer sidebar:**
   - ✅ Аватар с иконкой Shield
   - ✅ Отображение имени и роли
   - ✅ Sticky positioning внизу sidebar
   - ✅ Граница и отступы (border-t border-border)

4. **Переработан Dashboard с дизайн-системой:**
   - ✅ Удалён старый Header компонент (навигация теперь в layout)
   - ✅ Применены CSS-переменные ко всем stat cards
   - ✅ Использован `font-metrics` для всех чисел
   - ✅ Добавлен hover эффект на карточках (hover:shadow-md)
   - ✅ Иконки в цветных контейнерах (bg-primary/10)

5. **Статистические карточки (4 основные):**
   - ✅ Пользователи (с иконкой Users)
   - ✅ Здания (с иконкой Building2)
   - ✅ Обзоры (с иконкой MessageSquare)
   - ✅ Маршруты (с иконкой Route)
   - ✅ Все с единым дизайном (bg-card, border-border, rounded-[var(--radius)])

6. **Распределение ролей:**
   - ✅ Grid layout (2 на мобильных, 4 на десктопах)
   - ✅ Использован `font-metrics` для чисел ролей
   - ✅ Карточки с фоном (bg-muted/30)
   - ✅ Единый цвет для всех ролей (text-primary)

7. **Быстрые действия - расширено до 5:**
   - ✅ Управление пользователями
   - ✅ Модерация контента
   - ✅ Новости (новый)
   - ✅ Подкасты (новый)
   - ✅ Автогенерация
   - ✅ Применён hover эффект с border (hover:border-primary/50)
   - ✅ Group hover для иконок (group-hover:bg-primary/20)

8. **Loading states:**
   - ✅ Skeleton с дизайн-системой (bg-muted, rounded-[var(--radius)])
   - ✅ Правильная структура (flex-1 p-8)

**Сессия 4: Фаза 8 (частично) - Cleanup и документация**

1. **Очистка неиспользуемого кода:**
   - ✅ Удалены неиспользуемые импорты из AdminLayout (FileText, Building2)
   - ✅ Удалены неиспользуемые импорты из admin/page.tsx (FileText)
   - ✅ Удалена неиспользуемая функция getRoleColor из UserDropdown
   - ✅ Проверены старые удалённые страницы (saved-blogs, saved-news, liked-blogs, favorite-routes) - все удалены

2. **Исправление React Hook warnings:**
   - ✅ Обернул loadStats в useCallback в admin/page.tsx
   - ✅ Обернул loadUserStats в useCallback в UserDropdown.tsx
   - ✅ Добавлены правильные зависимости в useEffect

3. **ESLint статус:**
   - ✅ Все ошибки исправлены (0 errors)
   - ⚠️ Остались 2 предупреждения об использовании <img> в UserDropdown (не критично)

4. **Обновлена документация:**
   - ✅ Добавлена Сессия 3 (Фаза 7)
   - ✅ Добавлена Сессия 4 (Фаза 8 частично)
   - ✅ Обновлён статус Фазы 8

### 🔧 Технические детали исправлений:

**Проблема с blog_post_reactions:**
- PostgREST не мог найти relationship между `blog_post_reactions` и `blog_posts`
- Решение: Использованы два отдельных запроса вместо join:
  1. Получить реакции (post_id, created_at)
  2. Получить посты по массиву post_id
  3. Объединить данные на клиенте

**Проблема с news и routes:**
- Неправильные названия таблиц и колонок
- Решение:
  - `news_reactions` → `news_interactions`
  - `total_distance_km` → `distance_km`
  - `total_duration_minutes` → `estimated_duration_minutes`

---

## 📊 АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

### Проблемы

#### 1. **Хаос в структуре профиля**
Сейчас существует **12 разных страниц профиля**:
- `/profile` - главная страница профиля
- `/profile/edit` - редактирование профиля
- `/profile/settings` - настройки
- `/profile/buildings` - мои объекты
- `/profile/reviews` - мои отзывы
- `/profile/routes` - мои маршруты
- `/profile/articles` - мои статьи
- `/profile/favorites` - избранные маршруты
- `/profile/saved-blogs` - сохраненные блоги
- `/profile/saved-news` - сохраненные новости
- `/profile/liked-blogs` - лайкнутые блоги
- `/profile/favorite-routes` - избранные маршруты

**Проблема:** Дублирование, непонятная структура, нет единообразия.

#### 2. **Путаница с действиями**
Сейчас есть несколько типов взаимодействия с контентом:
- ❤️ **Like** (лайк) - на блогах, новостях, маршрутах, объектах
- 🔖 **Save** (сохранить) - на блогах, новостях
- 📚 **Add to Collection** (добавить в коллекцию) - на объектах, маршрутах
- ⭐ **Favorite** (избранное) - на маршрутах

**Проблема:** 4 разных действия делают одно и то же - "сохраняют для потом". Это запутывает пользователя.

#### 3. **Коллекции - нужна новая роль**
Текущая система коллекций (`user_collections`, `collection_items`):
- Не имеет четкого отличия от избранного
- Нет workflow "избранное → коллекция"
- Нельзя делиться коллекциями

**Новая роль коллекций:**
- **Избранное** = быстрое сохранение (лайк)
- **Коллекции** = организованные тематические подборки
- **Сценарий:** Лайкнул → в избранном → добавил в коллекцию "Поездка в Порто"
- **Шаринг:** Можно делиться коллекциями с другими

#### 4. **UserDropdown перегружен**
В выпадающем меню профиля сейчас **10+ ссылок**:
- Мой профиль
- Редактировать профиль
- Мои объекты (5)
- Мои отзывы (12)
- Мои маршруты (3)
- Избранные маршруты (2)
- Избранные блоги (4)
- Сохраненные блоги (2)
- Настройки
- Админ панель (для модераторов)
- Выход

**Проблема:** Слишком много пунктов, сложная навигация.

---

## 🎯 ПРЕДЛАГАЕМОЕ РЕШЕНИЕ

### Философия
**"Лайкнул → Сохранил → Нашёл"**

Простая и понятная логика:
1. Пользователь лайкает контент (блог, новость, маршрут, объект)
2. Лайкнутое автоматически попадает в "Избранное"
3. В избранном можно фильтровать по типу контента

### Архитектура

#### 1. **Единая система Избранного**

**Одно действие - ❤️ Лайк:**
- Убрать: Save (сохранить), Add to Collection, Favorite
- Оставить: Like (лайк) - универсальное действие для всего контента

**Две страницы:**

**1. `/profile/favorites` - Избранное (приватное)**
```
/profile/favorites
  ├── ?type=all (по умолчанию - всё лайкнутое)
  ├── ?type=blogs (блоги)
  ├── ?type=news (новости)
  ├── ?type=routes (маршруты)
  └── ?type=buildings (объекты)
```
- Кнопка "Добавить в коллекцию" на каждой карточке
- Быстрый доступ ко всему лайкнутому

**2. `/profile/collections` - Коллекции (можно шарить)**
```
/profile/collections
  ├── Список моих коллекций
  ├── /collections/[id] - детальная страница коллекции
  │   ├── Все элементы коллекции (блоги, здания, новости, маршруты)
  │   ├── Кнопка "Поделиться коллекцией"
  │   └── Кнопка "Удалить из коллекции"
  └── Кнопка "Создать коллекцию"
```

**База данных:**

**Оставить и улучшить:**
- `user_collections` - добавить поле `is_public` BOOLEAN, `share_token` UUID
- `collection_items` - оставить как есть
- Добавить: `building_favorites` для лайков объектов

**Использовать для лайков:**
- `blog_reactions` (reaction_type='like')
- `news_reactions` (reaction_type='like')
- `user_route_favorites` (уже есть)
- `building_favorites` (создать новую)

**Удалить:**
- `blog_reactions` где reaction_type='save'
- `news_reactions` где reaction_type='save'

#### 2. **Упрощенная структура профиля**

**Основные разделы:**
```
/profile
  ├── Обзор (главная страница профиля)
  │
  ├── Мой контент
  │   ├── /profile/buildings - Мои объекты
  │   ├── /profile/routes - Мои маршруты
  │   ├── /profile/reviews - Мои отзывы
  │   └── /profile/articles - Мои блог-посты
  │
  ├── Избранное и Коллекции
  │   ├── /profile/favorites?type=... - Всё лайкнутое с фильтрами (приватное)
  │   └── /profile/collections - Мои коллекции (можно шарить)
  │       └── /collections/[id] - Детальная страница коллекции
  │       └── /collections/[id]/[share-token] - Публичный доступ к коллекции
  │
  └── Настройки
      ├── /profile/edit - Редактирование профиля
      └── /profile/settings - Настройки аккаунта
```

**Страницы для удаления:**
- ❌ `/profile/saved-blogs`
- ❌ `/profile/saved-news`
- ❌ `/profile/liked-blogs`
- ❌ `/profile/favorite-routes`

#### 3. **Упрощенный UserDropdown**

**Новая структура меню:**
```
┌─────────────────────────────────┐
│ [Аватар] Имя Пользователя      │
│ @username                       │
├─────────────────────────────────┤
│ 👤 Мой профиль                  │
│ ✏️  Редактировать профиль       │
├─────────────────────────────────┤
│ 🏛️  Мои объекты (5)            │
│ 🛣️  Мои маршруты (3)           │
│ ✍️  Мои отзывы (12)            │
│ 📰 Мои статьи (8)               │
├─────────────────────────────────┤
│ ❤️  Избранное (24)              │
│ 📚 Коллекции (3)                │
├─────────────────────────────────┤
│ ⚙️  Настройки                   │
│ 🛡️  Админ панель (если админ)   │
├─────────────────────────────────┤
│ 🚪 Выход                        │
└─────────────────────────────────┘
```

**Счетчики:**
- Мои объекты: `buildings_count`
- Мои маршруты: `routes_count`
- Мои отзывы: `reviews_count`
- Мои статьи: `articles_count` (blog_posts)
- **Избранное: `favorites_total_count`** (сумма всех лайков)
- **Коллекции: `collections_count`**

#### 4. **Страница `/profile/favorites`**

**Дизайн:**
```
┌───────────────────────────────────────────────┐
│                                               │
│  ❤️ Избранное                                 │
│                                               │
│  [Все] [Блоги] [Новости] [Маршруты] [Объекты]│
│  ───────────────────────────────────────────  │
│                                               │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐      │
│  │ Блог 1  │  │ Новость │  │ Маршрут │      │
│  │ ❤️ 234  │  │ ❤️ 156  │  │ ❤️ 89   │      │
│  └─────────┘  └─────────┘  └─────────┘      │
│                                               │
└───────────────────────────────────────────────┘
```

**Функционал:**
- Табы для фильтрации по типу контента
- Единый дизайн карточек (как в `/blog`, `/news` и т.д.)
- Сортировка: по дате лайка, по популярности
- Поиск внутри избранного
- Кнопка "Убрать из избранного" (= снять лайк)
- **Кнопка "Добавить в коллекцию"** - открывает модалку выбора коллекции

#### 5. **Страница `/profile/collections`**

**Дизайн списка коллекций:**
```
┌───────────────────────────────────────────────┐
│                                               │
│  📚 Мои коллекции                             │
│                                     [Создать] │
│  ───────────────────────────────────────────  │
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │ 🏛️ Поездка в Порто              [🔗]  │ │
│  │ 12 элементов • Создана 5 дней назад    │ │
│  │ 3 блога, 5 зданий, 2 новости, 2 маршр.│ │
│  └─────────────────────────────────────────┘ │
│                                               │
└───────────────────────────────────────────────┘
```

**Детальная страница `/collections/[id]`:**
- Заголовок коллекции (редактируемый)
- Описание коллекции (опционально)
- Кнопка "Поделиться" → генерирует ссылку с share_token
- Переключатель "Публичная коллекция" (вкл/выкл)
- Все элементы коллекции (микс из блогов, зданий, новостей, маршрутов)
- Кнопка "Удалить из коллекции" на каждом элементе
- Кнопка "Удалить коллекцию"

**Публичный доступ `/collections/[id]/[share-token]`:**
- Любой пользователь (даже неавторизованный) может просмотреть
- Read-only режим (нельзя редактировать)
- Показывается автор коллекции
- Кнопка "Скопировать в мои коллекции" для авторизованных

---

## 🎨 ДИЗАЙН-СИСТЕМА

### Применение существующих паттернов

Использовать дизайн из **DESIGN_MIGRATION_PROGRESS.md**:

#### Компоненты
- **Tabs** - для фильтров типов (Все, Блоги, Новости, Маршруты, Объекты)
- **Cards** - BlogCard, NewsCard стили для карточек контента
- **Badges** - rounded-full для категорий и типов
- **Buttons** - `rounded-[var(--radius)]` для действий

#### Цвета
- **Primary** - для лайков и акцентов
- **bg-card** - фон карточек
- **border-border** - границы
- **text-foreground** - основной текст
- **text-muted-foreground** - вторичный текст

#### Шрифты
- **Rubik** (`font-sans`) - основной текст
- **Rubik** (`font-display`) - заголовки
- **Geologica** (`font-metrics`) - счетчики и метрики

---

## 🗄️ БАЗА ДАННЫХ

### Изменения

#### Таблицы для улучшения (коллекции остаются!)
```sql
-- Улучшить user_collections - добавить шаринг
ALTER TABLE user_collections ADD COLUMN IF NOT EXISTS is_public BOOLEAN DEFAULT false;
ALTER TABLE user_collections ADD COLUMN IF NOT EXISTS share_token UUID DEFAULT gen_random_uuid();
ALTER TABLE user_collections ADD COLUMN IF NOT EXISTS description TEXT;

-- Создать индекс для share_token
CREATE INDEX IF NOT EXISTS idx_user_collections_share_token ON user_collections(share_token);

-- RLS политика для публичных коллекций (по share_token)
CREATE POLICY "Anyone can view public collections with share token"
ON user_collections FOR SELECT
TO public
USING (is_public = true);

-- Коллекции можно просматривать по share_token даже если не авторизован
CREATE POLICY "Anyone can view collection by share token"
ON user_collections FOR SELECT
TO anon
USING (share_token IS NOT NULL);

COMMENT ON COLUMN user_collections.is_public IS 'Можно ли делиться коллекцией';
COMMENT ON COLUMN user_collections.share_token IS 'Уникальный токен для шаринга коллекции';
```

#### Таблицы для создания
```sql
-- Создать таблицу избранных объектов (по аналогии с user_route_favorites)
CREATE TABLE IF NOT EXISTS building_favorites (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  building_id UUID NOT NULL REFERENCES buildings(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, building_id)
);

-- RLS политики
ALTER TABLE building_favorites ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view their own favorites"
ON building_favorites FOR SELECT
TO authenticated
USING (auth.uid() = user_id);

CREATE POLICY "Users can add their own favorites"
ON building_favorites FOR INSERT
TO authenticated
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can remove their own favorites"
ON building_favorites FOR DELETE
TO authenticated
USING (auth.uid() = user_id);

-- Индекс для производительности
CREATE INDEX idx_building_favorites_user_id ON building_favorites(user_id);
CREATE INDEX idx_building_favorites_building_id ON building_favorites(building_id);
```

#### Обновление существующих таблиц
```sql
-- Удалить reaction_type='save' из blog_reactions и news_reactions
DELETE FROM blog_reactions WHERE reaction_type = 'save';
DELETE FROM news_reactions WHERE reaction_type = 'save';

-- Оставить только reaction_type='like'
-- Можно добавить constraint если нужно:
ALTER TABLE blog_reactions DROP CONSTRAINT IF EXISTS blog_reactions_type_check;
ALTER TABLE blog_reactions ADD CONSTRAINT blog_reactions_type_check
  CHECK (reaction_type = 'like');

ALTER TABLE news_reactions DROP CONSTRAINT IF EXISTS news_reactions_type_check;
ALTER TABLE news_reactions ADD CONSTRAINT news_reactions_type_check
  CHECK (reaction_type = 'like');
```

---

## 🔧 КОМПОНЕНТЫ

### 1. **FavoritesPage** (`/src/app/profile/favorites/page.tsx`)

Новый компонент страницы избранного.

**Структура:**
```typescript
interface FavoriteItem {
  id: string
  type: 'blog' | 'news' | 'route' | 'building'
  item_id: string
  created_at: string
  // + данные самого элемента (title, image, etc.)
}

export default function FavoritesPage() {
  const [activeTab, setActiveTab] = useState<'all' | 'blogs' | 'news' | 'routes' | 'buildings'>('all')
  const [favorites, setFavorites] = useState<FavoriteItem[]>([])
  const [loading, setLoading] = useState(true)

  // Загрузка избранного из всех 4 источников
  // Фильтрация по типу
  // Сортировка
  // Render карточек
}
```

### 2. **UpdatedUserDropdown** (`/src/components/UserDropdown.tsx`)

Обновить существующий компонент.

**Изменения:**
- Убрать: `liked_blogs_count`, `saved_blogs_count`, `collections_count`, `pending_requests_count`
- Добавить: `favorites_total_count`, `articles_count`
- Упростить навигацию до 9 пунктов

### 3. **LikeButton** (универсальный компонент)

Создать единый компонент для лайков всех типов контента.

```typescript
interface LikeButtonProps {
  type: 'blog' | 'news' | 'route' | 'building'
  itemId: string
  initialLiked?: boolean
  initialCount?: number
  className?: string
}

export default function LikeButton({ type, itemId, ... }) {
  // Универсальная логика лайков
  // Работает с blog_reactions, news_reactions, user_route_favorites, building_favorites
}
```

### 4. **Обновленные карточки**

Убрать кнопку "Save" / "Add to Collection", оставить только "Like":
- `BlogCard.tsx` - убрать Save
- `NewsCard.tsx` - убрать Save
- Карточки маршрутов - заменить Favorite на Like
- Карточки объектов - убрать Add to Collection, добавить Like

---

## 🚀 ПЛАН МИГРАЦИИ

### Фаза 1: Подготовка ✅ ЗАВЕРШЕНО (1-2 часа)
- [x] Создать документ PROFILE_REDESIGN.md
- [x] Утвердить план с пользователем
- [x] Создать новую ветку git: `feature/profile-redesign`

### Фаза 2: База данных ✅ ЗАВЕРШЕНО (2-3 часа)
- [x] Создать миграцию `032_enhance_favorites_and_collections.sql`
- [x] Создать таблицу `building_favorites` с RLS политиками
- [x] **Улучшить user_collections** - добавить is_public, share_token, description
- [x] Создать RLS политики для публичных коллекций
- [x] Очистить `blog_post_reactions` от 'save' (только 'like')
- [x] Применить миграцию успешно
- [x] Проверить целостность данных

### Фаза 3: Компоненты избранного ✅ ЗАВЕРШЕНО (3-4 часа)
- [x] Создать универсальный `LikeButton` компонент (src/components/LikeButton.tsx)
- [x] Создать страницу `/profile/favorites` с фильтрами (все типы: blog, news, route, building)
- [x] Обновить карточки (BlogCard, NewsCard) - интегрирован LikeButton
- [x] Удалить старые страницы (saved-blogs, saved-news, liked-blogs, favorite-routes)
- [x] Обновить UserDropdown - новая структура навигации
- [x] **ИСПРАВЛЕНО:** Все запросы к базе данных для избранного работают корректно
- [x] **ИСПРАВЛЕНО:** Отображение всех типов лайков (блоги, новости, маршруты, здания)
- [ ] Создать модалку `AddToCollectionModal` (выбор коллекции) - ОТЛОЖЕНО

### Фаза 3.5: Дизайн-адаптация профильных страниц ✅ ЗАВЕРШЕНО (2-3 часа)
- [x] Унифицирован отступ от header (pt-10) на всех страницах профиля
- [x] Исправлен footer на всех страницах (sticky footer pattern)
- [x] Применены CSS-переменные везде (bg-card, border-border, etc.)
- [x] Убрано желтое выделение на /profile/reviews
- [x] Добавлен grid режим на /profile/reviews
- [x] Переименование "Мои статьи" → "Мои блоги"
- [x] Функционал архивации только для админов
- [x] Возможность разархивации блогов

### Фаза 4: Компоненты коллекций ✅ ЗАВЕРШЕНО (4-5 часов)
- [x] Обновить страницу `/profile/collections` (список коллекций) - УЖЕ БЫЛА
- [x] Создать страницу `/collections/[id]` (детальная страница) - УЖЕ БЫЛА
- [x] Создать страницу `/collections/[id]/[share-token]` (публичный доступ)
- [x] Создать модалку `CreateCollectionModal` - ВСТРОЕНА в ProfileCollectionsPage
- [x] Создать модалку `ShareCollectionModal` (генерация ссылки)
- [x] Добавить функционал "Скопировать коллекцию" - в PublicCollectionPage

### Фаза 5: Навигация и UserDropdown ✅ ЗАВЕРШЕНО (2-3 часа)
- [x] Обновить `UserDropdown` - новая структура меню (10 пунктов)
- [x] Добавить счетчики: favorites_total_count, collections_count - УЖЕ БЫЛИ
- [ ] Настроить редиректы со старых URL на новые - НЕ ТРЕБУЕТСЯ (старые удалены)
- [ ] Обновить мобильное меню Header - ПРОПУЩЕНО (не приоритетно)

### Фаза 6: Тестирование (3-4 часа)
- [ ] Проверить работу лайков на всех типах контента
- [ ] Проверить страницу избранного с фильтрами
- [ ] Проверить создание коллекций
- [ ] Проверить добавление в коллекцию из избранного
- [ ] Проверить шаринг коллекций (публичные ссылки)
- [ ] Проверить "Скопировать коллекцию"
- [ ] Проверить UserDropdown и навигацию
- [ ] Проверить адаптивность на мобильных
- [ ] Проверить что старые URL редиректят

### Фаза 7: Админ панель с аналитикой ✅ ЗАВЕРШЕНО (3-4 часа)
- [x] Структурировать `/admin` - создан AdminLayout с sidebar
- [x] Создать единый дашборд - переработан с дизайн-системой
- [x] Упорядочить разделы модерации - группировка в навигации
- [x] Применить дизайн-систему - полностью применена (CSS-переменные, font-metrics, rounded-[var(--radius)])

### Фаза 8: Финальная полировка ⚙️ ЧАСТИЧНО (1-2 часа)
- [x] Обновить документацию - добавлена Сессия 3 (Фаза 7)
- [x] Очистить неиспользуемый код:
  - Удалены неиспользуемые импорты (FileText, Building2 из AdminLayout)
  - Удалена неиспользуемая функция getRoleColor из UserDropdown
  - Исправлены React Hook warnings (добавлены useCallback)
  - Все ESLint ошибки исправлены
- [ ] Проверить производительность
- [ ] Code review
- [ ] Merge в main

**Общее время: ~20-25 часов** (с учетом коллекций и аналитики)

---

## 📝 АДМИН ПАНЕЛЬ - СТРУКТУРА

### Текущие проблемы
- Разрозненные страницы: `/admin`, `/admin/moderation`, `/admin/users`
- Нет единого дашборда
- Нет навигации между разделами

### Предлагаемая структура

```
/admin
  ├── Dashboard (главная)
  │   ├── Статистика (всего пользователей, объектов, маршрутов)
  │   ├── Недавняя активность
  │   └── Быстрые действия
  │
  ├── Модерация (/admin/moderation)
  │   ├── Объекты на модерации
  │   ├── Маршруты на модерации
  │   └── Отзывы на модерации
  │
  ├── Пользователи (/admin/users)
  │   ├── Список пользователей
  │   ├── Роли и права
  │   └── Заблокированные
  │
  ├── Контент (/admin/content)
  │   ├── Блоги
  │   ├── Новости
  │   └── Подкасты
  │
  └── Настройки (/admin/settings)
      ├── Общие настройки
      └── Управление доступом
```

**Дизайн:**
- Sidebar с навигацией (скрытый на мобильных)
- Единый layout для всех админ страниц
- Применить дизайн-систему (bg-card, border-border и т.д.)
- Stats cards с `font-metrics` для цифр
- Таблицы с сортировкой и фильтрацией

---

## ✅ ОТВЕТЫ НА ВОПРОСЫ (получены от пользователя)

1. **Коллекции:**
   - ✅ **ОСТАВИТЬ** с новой логикой
   - **Сценарий:** Пользователь лайкает контент → попадает в избранное → из избранного добавляет в коллекцию
   - **Пример:** Планирование поездки в Порто - собрать блоги, здания, новости в одну коллекцию "Поездка в Порто"
   - **Шаринг:** Коллекциями можно делиться

2. **Статьи пользователя:**
   - ✅ Это статьи блога (`blog_posts`), созданные пользователем
   - Страница `/profile/articles` для управления своими блог-постами

3. **Избранное - приватность:**
   - ✅ Избранное приватное (только для владельца)
   - ✅ Коллекции можно делать публичными и шарить

4. **Уведомления:**
   - ✅ Только для комментариев
   - ❌ НЕ отправлять при лайках

5. **Миграция данных:**
   - ✅ Не важна, сайт в тестовом режиме
   - Можно просто удалить старые "save"

6. **Админ панель - Аналитика:**
   - ✅ Нужна
   - **Предлагаемые метрики:**
     - 📊 Общая статистика (пользователи, объекты, маршруты, контент)
     - 📈 Графики роста по дням/неделям/месяцам
     - 🔥 Топ контента (самые популярные объекты, маршруты, статьи)
     - 👥 Активные пользователи (за день/неделю/месяц)
     - 🗺️ География (города, страны с наибольшим контентом)
     - ⚡ Активность (новые объекты/маршруты/отзывы за период)
     - 🎯 Конверсии (регистрации → создание контента → публикация)

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

### Для пользователя
- ✅ Простая и понятная система: "Лайкнул → Сохранил → Нашёл"
- ✅ Одна страница избранного вместо 4+ разных
- ✅ Меньше кликов для доступа к контенту
- ✅ Чистый и структурированный профиль
- ✅ Единообразный дизайн

### Для разработки
- ✅ Меньше кода (удаление коллекций)
- ✅ Проще поддержка (один тип взаимодействия)
- ✅ Меньше таблиц в БД
- ✅ Единый компонент лайков
- ✅ Лучшая производительность

### Для модераторов
- ✅ Структурированная админ панель
- ✅ Единый дашборд
- ✅ Быстрый доступ ко всем функциям

---

## 📚 ССЫЛКИ

- [DESIGN_MIGRATION_PROGRESS.md](./DESIGN_MIGRATION_PROGRESS.md) - дизайн-система
- [CLAUDE.md](./CLAUDE.md) - архитектурные паттерны проекта
- Design/src/pages/Settings.tsx - референс дизайна настроек

---

---

## 📋 КРАТКИЙ SUMMARY ПРОГРЕССА

### ✅ Завершённые фазы:
- **Фаза 1:** Подготовка и планирование
- **Фаза 2:** База данных (building_favorites, user_collections улучшения)
- **Фаза 3:** Компоненты избранного (LikeButton, ProfileFavoritesPage)
- **Фаза 3.5:** Дизайн-адаптация профильных страниц
- **Фаза 4:** Компоненты коллекций (публичный доступ, шаринг)
- **Фаза 5:** Навигация и UserDropdown (упрощение структуры)
- **Фаза 7:** Админ панель с дизайн-системой (sidebar, dashboard)
- **Фаза 8 (частично):** Cleanup и документация

### 🔄 В процессе:
- **Фаза 6:** Тестирование всех функций
- **Фаза 8:** Производительность, code review, merge

### 📊 Статистика:
- **Созданные компоненты:** LikeButton, ProfileFavoritesPage, ShareCollectionModal, PublicCollectionPage, CollectionsBadgeDropdown
- **Обновлённые компоненты:** UserDropdown, AdminLayout, Admin Dashboard, BlogCard, NewsCard
- **Миграции БД:** 1 (032_enhance_favorites_and_collections.sql)
- **Удалённые страницы:** 4 (saved-blogs, saved-news, liked-blogs, favorite-routes)
- **Применённые паттерны:** CSS-переменные, font-metrics, rounded-[var(--radius)], useCallback hooks

### 🎯 Ключевые достижения:
- ✅ Единая система избранного с фильтрами
- ✅ Публичные коллекции с share-token
- ✅ Упрощённая навигация (10 пунктов вместо 12+)
- ✅ Админ-панель с дизайн-системой
- ✅ Чистый код без ESLint ошибок

---

## ✅ ПРОГРЕСС РЕАЛИЗАЦИИ

### Что уже сделано:

#### 1. **База данных** ✅
- ✅ Создана таблица `building_favorites` (user_id, building_id, created_at)
- ✅ Добавлены колонки в `user_collections`:
  - `is_public` BOOLEAN - флаг публичности
  - `share_token` UUID - токен для шаринга
  - `description` TEXT - описание коллекции
- ✅ RLS политики:
  - `building_favorites` - только владелец может управлять
  - `user_collections` - публичные коллекции доступны по share_token
  - `collection_items` - элементы публичных коллекций видны всем
- ✅ Удалены 'save' реакции из `blog_post_reactions`, оставлены только 'like'
- ✅ Созданы функции:
  - `get_user_favorites_count(user_id)` - общий счетчик избранного
  - `get_collection_stats(collection_id)` - статистика коллекции

#### 2. **Компоненты** ✅
- ✅ **LikeButton.tsx** - универсальная кнопка лайка
  - Поддержка всех типов: blog, news, route, building
  - Два варианта: compact (иконка + число) и default (полная кнопка)
  - Автоматическая работа с нужными таблицами
  - Optimistic updates

- ✅ **ProfileFavoritesPage.tsx** - страница избранного
  - Фильтры по типам: Все, Блоги, Новости, Маршруты, Здания
  - Загрузка из всех 4 источников параллельно
  - Использует BlogCard и NewsCard для отображения
  - Отдельные карточки для маршрутов и зданий
  - Responsive дизайн
  - **ИСПРАВЛЕНО:** Два отдельных запроса для блогов (обход PostgREST cache issue)
  - **ИСПРАВЛЕНО:** Правильные названия таблиц для news и routes
  - **ПРОТЕСТИРОВАНО:** Все типы контента отображаются корректно

- ✅ **BlogCard.tsx & NewsCard.tsx** - обновлены
  - Убрана кнопка "Save"
  - Интегрирован LikeButton в compact режиме
  - Статичные счетчики заменены на интерактивные

#### 3. **Навигация** ✅
- ✅ **UserDropdown.tsx** - упрощен
  - Удалены: "Избранные маршруты", "Избранные блоги", "Сохраненные блоги"
  - Добавлены: "Избранное" (единое), "Мои статьи"
  - Счетчик избранного суммирует все лайки
  - Структура: 10 пунктов вместо 12+

#### 4. **Удаленные страницы** ✅
- ✅ /profile/saved-blogs
- ✅ /profile/saved-news
- ✅ /profile/liked-blogs
- ✅ /profile/favorite-routes

### Что осталось сделать:

#### 1. **Дизайн-адаптация** ✅ ЗАВЕРШЕНО
- [x] Проверить соответствие Design/src/pages/Settings.tsx
- [x] Унифицировать отступы и spacing
- [x] Исправить footer на всех страницах
- [x] Применить CSS-переменные везде
- [x] Убрать проблемные hover эффекты
- [x] Добавить grid режим на /profile/reviews

#### 2. **Модалка AddToCollectionModal** 📦
- [ ] Создать компонент выбора коллекции
- [ ] Добавить кнопку "Добавить в коллекцию" в карточки избранного
- [ ] Функционал создания новой коллекции

#### 3. **Страница коллекций** 📚
- [ ] /profile/collections - список коллекций
- [ ] /collections/[id] - детальная страница
- [ ] /collections/[id]/[share-token] - публичный доступ
- [ ] Модалка CreateCollectionModal
- [ ] Модалка ShareCollectionModal

#### 4. **Тестирование** 🧪
- [ ] Проверка лайков на всех типах контента
- [ ] Проверка фильтров в избранном
- [ ] Проверка счетчиков в UserDropdown
- [ ] Мобильная адаптивность

---

## 🎨 ДИЗАЙН-АДАПТАЦИЯ (План)

### Проверка соответствия Design/

#### Анализ референсного дизайна:

**Design/src/pages/Settings.tsx:**
- ✅ Использует Tabs компонент с иконками
- ✅ Применяет `rounded-[var(--radius)]` для кнопок
- ✅ Использует `bg-card`, `border-border`, `text-muted-foreground`
- ✅ Separator между секциями
- ✅ Grid layout для опций (3 колонки)

**Design/src/components/BlogCard.tsx:**
- ✅ `bg-card` для фона карточек
- ✅ `hover:-translate-y-1 hover:shadow-lg` для hover
- ✅ `border-t-2 border-border` для разделителей
- ✅ Icons: Calendar, Clock, Eye, MessageCircle (3.5x3.5)
- ✅ `line-clamp-2` для заголовков, `line-clamp-3` для описания

**Design/src/components/NewsCard.tsx:**
- ✅ Badge компонент для категорий (`bg-news-primary`)
- ✅ `group-hover:scale-105` для изображений
- ✅ `transition-transform duration-500`
- ✅ Adaptive: `md:flex-row`, `md:w-2/5`

#### Применение к /profile/favorites:

**Текущая реализация:**
```typescript
// ✅ Уже применено:
- bg-background для страницы
- bg-card для фильтров
- border-border для границ
- rounded-[var(--radius)] для кнопок
- text-muted-foreground для счетчиков
- font-heading для заголовков
- Adaptive grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
```

**Нужно улучшить:**
1. **Фильтры** - использовать Tabs компонент вместо кнопок
2. **Hover эффекты** - добавить `-translate-y-1` для карточек
3. **Spacing** - проверить соответствие gap-6, p-4, mb-8
4. **Typography** - применить `font-display` к заголовкам

### План доработки дизайна:

#### Этап 1: Tabs вместо кнопок фильтров
```typescript
// Заменить:
<div className="flex flex-wrap gap-2">
  {filters.map(...)}
</div>

// На Tabs компонент:
<Tabs defaultValue="all" className="w-full">
  <TabsList className="grid w-full grid-cols-5">
    <TabsTrigger value="all">
      <Heart className="h-4 w-4" />
      <span>Все</span>
    </TabsTrigger>
    // ...
  </TabsList>
</Tabs>
```

#### Этап 2: Hover эффекты для карточек
```typescript
// Добавить к Link карточкам:
className="... hover:-translate-y-1 transition-transform duration-300"
```

#### Этап 3: Улучшить пустое состояние
```typescript
// Добавить gradient background:
<div className="text-center py-16 bg-gradient-to-br from-primary/5 to-transparent rounded-lg">
  <Heart className="w-20 h-20 text-muted-foreground/50 mx-auto mb-4" />
  // ...
</div>
```

#### Этап 4: Добавить loading skeletons
```typescript
// Заменить простые div на Skeleton компоненты
<Skeleton className="h-48 w-full" />
<Skeleton className="h-4 w-3/4" />
```

---

## 🎬 КРАТКИЙ SUMMARY

### Что делаем:
1. ✅ **Лайк вместо Save** - одно действие для всего контента
2. ✅ **Избранное** - приватная страница со всем лайкнутым (с фильтрами)
3. ✅ **Коллекции** - тематические подборки, можно шарить
4. ✅ **UserDropdown** - 10 пунктов вместо 12+
5. ✅ **Админ панель** - с дашбордом и аналитикой

### Workflow:
```
Контент → Лайк → Избранное → Добавить в коллекцию → Поделиться
```

### Ключевые фичи:
- 📱 Публичные коллекции по share-token
- 📊 Аналитика в админке (графики, топы, география)
- 🎨 Единый дизайн по DESIGN_MIGRATION_PROGRESS.md
- ❤️ Универсальный LikeButton для всех типов
- 📚 Коллекции-подборки для планирования поездок
