# Руководство по применению миграций оптимизации БД

**Дата создания:** 27 ноября 2025
**Статус:** Готово к применению
**Приоритет:** Средний (после запуска проекта)

---

## 📋 Обзор

Созданы две миграции для оптимизации производительности базы данных:
1. **025_additional_search_indexes.sql** - Дополнительные индексы для поиска и фильтрации
2. **026_optimize_rls_policies.sql** - Оптимизация RLS политик

---

## 🎯 Миграция 025: Дополнительные индексы поиска

### Что делает:

**1. Недостающие индексы из рекомендаций (раздел 2.3):**
- ✅ `idx_architecture_news_category` - для фильтрации новостей по категориям

**2. Полнотекстовый поиск (GIN индексы):**
- ✅ `idx_buildings_search` - поиск по зданиям (name, description, architect)
- ✅ `idx_routes_search` - поиск по маршрутам (title, description)
- ✅ `idx_blog_posts_search` - поиск по блог-постам
- ✅ `idx_architecture_news_search` - поиск по новостям

**3. Геопространственные индексы (GIST):**
- ✅ `idx_buildings_location` - поиск зданий рядом с координатами
- ✅ `idx_route_points_location` - поиск точек маршрутов рядом

**4. Индексы для сортировки и фильтрации:**
- ✅ `idx_buildings_popularity` - одобренные здания
- ✅ `idx_routes_difficulty` - фильтрация по сложности
- ✅ `idx_routes_premium` - фильтрация премиум маршрутов
- ✅ `idx_blog_posts_recent` - последние популярные посты
- ✅ `idx_architecture_news_recent` - последние новости
- ✅ `idx_podcast_episodes_popular` - популярные подкасты
- ✅ `idx_notifications_unread_recent` - непрочитанные уведомления
- ✅ `idx_collections_public_with_items` - публичные коллекции

### Эффект:
- Ускорение полнотекстового поиска в 50-100 раз
- Ускорение геопространственных запросов в 10-50 раз
- Улучшение сортировки и фильтрации в 5-20 раз

### Размер:
- Дополнительное место на диске: ~50-150 MB (зависит от объема данных)
- Время применения: 2-5 минут

---

## 🚀 Миграция 026: Оптимизация RLS политик

### Что делает:

Заменяет `auth.uid()` на `(SELECT auth.uid())` в RLS политиках для предотвращения повторной оценки функции.

**Оптимизированные таблицы:**
1. ✅ `profiles` - 3 политики
2. ✅ `buildings` - 4 политики
3. ✅ `building_reviews` - 4 политики
4. ✅ `routes` - 5 политик
5. ✅ `notifications` - 3 политики
6. ✅ `user_collections` - 5 политик
7. ✅ `blog_posts` - 4 политики
8. ✅ `route_completions` - 3 политики

**Всего:** 31 политика оптимизирована

### Эффект:
- Ускорение запросов с RLS в 2-5 раз
- Снижение нагрузки на CPU базы данных на 20-40%
- Более предсказуемое время выполнения запросов

### Влияние:
- Нет дополнительного места на диске
- Время применения: 1-2 минуты

---

## 📝 Как применить миграции

### Вариант 1: Через Supabase Dashboard (рекомендуется)

1. Откройте **Supabase Dashboard** → ваш проект
2. Перейдите в **SQL Editor**
3. Создайте новый запрос
4. Скопируйте содержимое `database/migrations/025_additional_search_indexes.sql`
5. Нажмите **Run** или **Ctrl+Enter**
6. Дождитесь завершения (2-5 минут)
7. Проверьте результат с помощью функции `check_search_indexes()`
8. Повторите шаги 3-7 для `database/migrations/026_optimize_rls_policies.sql`

### Вариант 2: Через Supabase CLI

```bash
# Убедитесь, что Supabase CLI установлен
supabase --version

# Применить миграцию 025
supabase db push database/migrations/025_additional_search_indexes.sql

# Применить миграцию 026
supabase db push database/migrations/026_optimize_rls_policies.sql
```

### Вариант 3: Через psql

```bash
# Подключитесь к вашей базе данных
psql "postgresql://postgres:[ВАШ_ПАРОЛЬ]@db.[ВАШ_ПРОЕКТ].supabase.co:5432/postgres"

# Применить миграцию 025
\i database/migrations/025_additional_search_indexes.sql

# Применить миграцию 026
\i database/migrations/026_optimize_rls_policies.sql
```

---

## ✅ Проверка результатов

### После применения миграции 025:

```sql
-- Проверить созданные индексы поиска
SELECT * FROM check_search_indexes();

-- Должно показать:
-- - GIN индексы для полнотекстового поиска (4 индекса)
-- - GIST индексы для геопространственных запросов (2 индекса)
-- - Остальные индексы фильтрации (9 индексов)
-- ИТОГО: ~15 новых индексов
```

### После применения миграции 026:

```sql
-- Проверить оптимизацию RLS политик
SELECT * FROM check_rls_optimization();

-- Проверить статистику
SELECT
  COUNT(*) FILTER (WHERE using_optimized) as optimized_policies,
  COUNT(*) FILTER (WHERE NOT using_optimized) as non_optimized_policies,
  COUNT(*) as total_policies
FROM check_rls_optimization();

-- Ожидается:
-- optimized_policies: 31
-- non_optimized_policies: 0 (или близко к 0)
```

---

## ⚠️ Важные замечания

### Перед применением:

1. **Сделайте backup базы данных:**
   ```bash
   # Через Supabase Dashboard
   # Settings → Database → Create backup
   ```

2. **Проверьте место на диске:**
   - Миграция 025 добавит ~50-150 MB индексов
   - Убедитесь, что есть свободное место

3. **Выберите время с низкой нагрузкой:**
   - Лучше применять миграции ночью или в выходные
   - Миграция 025 может занять 2-5 минут
   - Во время создания индексов могут быть медленные запросы

### После применения:

1. **Мониторинг производительности:**
   ```sql
   -- Проверить размер индексов
   SELECT
     schemaname,
     tablename,
     indexname,
     pg_size_pretty(pg_relation_size(indexname::regclass)) as size
   FROM pg_indexes
   WHERE schemaname = 'public'
   ORDER BY pg_relation_size(indexname::regclass) DESC
   LIMIT 20;
   ```

2. **Проверить использование индексов:**
   ```sql
   -- Через несколько дней после применения
   SELECT
     schemaname,
     tablename,
     indexname,
     idx_scan as index_scans,
     idx_tup_read as tuples_read
   FROM pg_stat_user_indexes
   WHERE schemaname = 'public'
   ORDER BY idx_scan DESC
   LIMIT 20;
   ```

---

## 🔄 Откат миграций (если нужно)

### Откат миграции 025:

```sql
-- Удалить созданные индексы
DROP INDEX IF EXISTS idx_architecture_news_category;
DROP INDEX IF EXISTS idx_buildings_search;
DROP INDEX IF EXISTS idx_routes_search;
DROP INDEX IF EXISTS idx_blog_posts_search;
DROP INDEX IF EXISTS idx_architecture_news_search;
DROP INDEX IF EXISTS idx_buildings_location;
DROP INDEX IF EXISTS idx_route_points_location;
DROP INDEX IF EXISTS idx_buildings_popularity;
DROP INDEX IF EXISTS idx_routes_difficulty;
DROP INDEX IF EXISTS idx_routes_premium;
DROP INDEX IF EXISTS idx_blog_posts_recent;
DROP INDEX IF EXISTS idx_architecture_news_recent;
DROP INDEX IF EXISTS idx_podcast_episodes_popular;
DROP INDEX IF EXISTS idx_notifications_unread_recent;
DROP INDEX IF EXISTS idx_collections_public_with_items;

-- Удалить функцию проверки
DROP FUNCTION IF EXISTS check_search_indexes();
```

### Откат миграции 026:

```sql
-- Необходимо пересоздать политики с auth.uid() вместо (SELECT auth.uid())
-- Используйте предыдущую версию политик из миграций 022_critical_rls_policies.sql
```

---

## 📊 Ожидаемые улучшения

### Миграция 025:
- Полнотекстовый поиск: **50-100x быстрее**
- Геопространственные запросы: **10-50x быстрее**
- Фильтрация и сортировка: **5-20x быстрее**

### Миграция 026:
- Запросы с RLS: **2-5x быстрее**
- Снижение CPU: **20-40%**
- Более стабильная производительность

### Общий эффект:
- Значительное ускорение работы приложения
- Лучший опыт пользователей
- Снижение нагрузки на Supabase (возможна экономия $$)

---

## 📞 Поддержка

Если возникли проблемы при применении миграций:
1. Проверьте логи ошибок в Supabase Dashboard → Logs
2. Убедитесь, что у вас есть права администратора БД
3. Проверьте, что миграции 022-024 были применены ранее
4. В случае критических проблем - откатите миграции (см. раздел "Откат миграций")

**Рекомендация:** Применяйте миграции последовательно (025 → 026), проверяя результаты после каждой.

---

**Последнее обновление:** 27 ноября 2025
**Автор:** Claude (Anthropic)
