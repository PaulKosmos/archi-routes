# =============================================================
# Archi-Routes — Environment Variables
# Copy this file to .env.local and fill in the values
# =============================================================

# --- SUPABASE (Required) ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # Server-side only, never expose to client

# --- MAPBOX (Required) ---
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your-mapbox-token

# --- SITE URL (Optional, defaults to https://archi-routes.com) ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- PASSWORD GATE (Required for gated access) ---
SITE_ACCESS_PASSWORD=your-site-password

# --- ROUTING SERVICES (Optional) ---
# OpenRouteService — alternative routing provider
NEXT_PUBLIC_ORS_API_KEY=your-ors-api-key

# Google Maps — alternative geocoding
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-key

# --- MONITORING (Optional) ---
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
