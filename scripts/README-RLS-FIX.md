# 🔧 Исправление проблемы автогенерации маршрутов

## 🎯 Проблема
Автогенерация маршрутов не работает с ошибкой:
```
new row violates row-level security policy for table "routes"
```

## ✅ Решение

### 1. Выполните SQL скрипт
Выполните файл `scripts/fix-routes-rls.sql` в вашей базе данных Supabase:

```sql
-- Скопируйте содержимое файла scripts/fix-routes-rls.sql
-- и выполните в SQL Editor вашего Supabase проекта
```

### 2. Проверьте переменные окружения
Убедитесь, что в `.env.local` есть:
```env
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
OPENAI_API_KEY=your_openai_key  # для автогенерации
```

### 3. Перезапустите приложение
```bash
npm run dev
```

## 🔍 Что исправляет скрипт

1. **RLS политики для routes** - позволяет авторизованным пользователям создавать маршруты
2. **RLS политики для route_points** - позволяет управлять точками маршрутов
3. **Таблицы автогенерации** - создает недостающие таблицы, если их нет
4. **Базовые данные** - добавляет шаблоны и AI провайдеров
5. **Индексы** - для оптимизации запросов

## 🎉 Результат

После выполнения скрипта автогенерация маршрутов должна работать:
- ✅ Кнопка "Сгенерировать маршрут с ИИ" создает маршруты
- ✅ Маршруты сохраняются в базу данных  
- ✅ Пользователь видит свои созданные маршруты
- ✅ Система работает без ошибок RLS

## 🚨 Если проблема остается

1. Проверьте логи в консоли браузера
2. Проверьте логи в Supabase Dashboard
3. Убедитесь, что пользователь авторизован при создании маршрута
4. Проверьте правильность Service Role Key

## 📋 Проверка работы

Попробуйте создать маршрут через автогенерацию:
1. Нажмите "Создать маршрут" 
2. Выберите "Автогенерация с ИИ"
3. Заполните параметры
4. Нажмите "Сгенерировать маршрут с ИИ"
5. Должно появиться сообщение об успешном создании