# 👤 Назначение пользователя модератором

## Быстрое назначение Алины модератором

### Способ 1: Через SQL (Рекомендуемый)

1. **Откройте Supabase Dashboard**
2. **Перейдите в SQL Editor**
3. **Выполните скрипт**: `scripts/assign-moderator.sql`

Этот скрипт:
- ✅ Найдет пользователя по email `alina.chebakova95@gmail.com`
- ✅ Назначит роль `moderator`
- ✅ Установит статус верификации
- ✅ Покажет результат

### Способ 2: Через административную функцию

1. **Выполните**: `scripts/create-assign-role-function.sql`
2. **Используйте функцию**:
```sql
SELECT assign_user_role('alina.chebakova95@gmail.com', 'moderator', 'Назначен модератором администратором');
```

### Способ 3: Через веб-интерфейс (для админов)

1. **Войдите как администратор**
2. **Откройте выпадающее меню профиля**
3. **Перейдите в "Управление пользователями"** (`/admin/users`)
4. **Найдите пользователя Алину**
5. **Нажмите "Изменить роль"**
6. **Выберите "Модератор"**

## 🚨 Важно

**Пользователь должен сначала зарегистрироваться!**

Если Алина еще не зарегистрировалась:
1. Она должна создать аккаунт на платформе
2. Подтвердить email
3. Только потом можно назначить роль модератора

## Проверка результата

После назначения проверьте в Supabase:

```sql
SELECT 
    u.email,
    p.role,
    p.is_verified,
    p.verification_notes
FROM auth.users u
LEFT JOIN profiles p ON u.id = p.id
WHERE u.email = 'alina.chebakova95@gmail.com';
```

Должно показать:
- `role`: `moderator`
- `is_verified`: `true`

## Права модератора

После назначения Алина получит доступ к:

### В выпадающем меню профиля:
- 🔧 **Модерация** (`/admin/moderation`)
- 🤖 **Автогенерация** (`/admin/autogeneration`)

### Возможности:
- ✅ Модерация маршрутов и контента
- ✅ Просмотр статистики платформы
- ✅ Создание и управление маршрутами
- ✅ Автогенерация контента
- ✅ Доступ к админ-панелям

## Управление ролями

### Доступные роли:
- **guest** - Гость (ограниченный доступ)
- **explorer** - Исследователь (базовый пользователь)
- **guide** - Гид (может создавать маршруты)
- **expert** - Эксперт (профессионал архитектуры)
- **moderator** - Модератор (управление контентом)
- **admin** - Администратор (полный доступ)

### Иерархия прав:
```
Admin > Moderator > Expert > Guide > Explorer > Guest
```

### Автоматическая верификация:
При назначении ролей `moderator`, `admin`, `expert` пользователь автоматически получает статус `is_verified = true`.

## Массовое назначение ролей

Для назначения нескольких пользователей используйте функцию:

```sql
-- Назначить роль эксперта
SELECT assign_user_role('expert@example.com', 'expert', 'Профессиональный архитектор');

-- Назначить роль гида
SELECT assign_user_role('guide@example.com', 'guide', 'Активный создатель маршрутов');

-- Назначить роль модератора
SELECT assign_user_role('moderator@example.com', 'moderator', 'Ответственный за модерацию');
```

## Отзыв роли

Для понижения роли:

```sql
SELECT assign_user_role('user@example.com', 'explorer', 'Роль понижена до исследователя');
```

---

**Выполните нужный способ и Алина станет модератором!** ✨
