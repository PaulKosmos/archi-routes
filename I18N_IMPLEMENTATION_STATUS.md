# Статус реализации i18n/l10n

**Дата:** 2025-12-03
**Версия:** 0.1.4
**Статус сайта:** ✅ Полностью восстановлен и работает
**Статус i18n:** ⏸️ Отложено до pre-launch

---

## 🎯 ФИНАЛЬНОЕ РЕШЕНИЕ (03.12.2025)

После анализа проблем и оценки рисков принято решение:

### Текущее состояние (v0.1.4 - Pre-launch)
✅ **Оставляем всё как есть:**
- Сайт работает стабильно на русском языке
- База данных поддерживает многоязычный контент (миграция 030 применена)
- Компоненты перевода готовы (`TranslationFields.tsx`)
- next-intl установлен, но не активирован

### План миграции на английский (Pre-launch)
📋 **Перед запуском:**
1. Перевести весь интерфейс на английский язык
2. Перевести контент в БД (новости, описания зданий, маршруты)
3. Запустить сайт на английском как основной язык
4. Отложить многоязычность до v1.0

### План полной локализации (Post-launch, v1.0+)
🚀 **После запуска и стабилизации:**
1. Полная миграция на `[locale]` routing (Вариант 2)
2. Реализация локализованных URL (`/en/news`, `/de/buildings`, `/ru/profile`)
3. Добавление языковых переключателей
4. SEO оптимизация для разных языков

### Причины решения
- ✅ Минимизация рисков перед запуском
- ✅ Стабильность приоритетнее многоязычности
- ✅ Английский язык - первичная аудитория
- ✅ Полная локализация требует 12-20 часов работы
- ✅ Лучше запустить стабильный продукт, чем откладывать ради feature

---

## 🔧 Последнее обновление (02.12.2025 - 22:26)

**Проблема:** После первоначального отката сайт снова перестал работать из-за неполного удаления директории `app/[locale]/`.

**Ошибка:**
```
Module not found: Can't resolve '@/i18n/routing'
GET / 500 in 728ms
```

**Причина:** Директория `src/app/[locale]/` с файлами `layout.tsx` и `page.tsx` осталась после отката, что вызывало конфликт роутинга и попытку импорта несуществующих модулей i18n.

**Решение:**
1. Остановлен dev сервер
2. Полностью удалена директория `src/app/[locale]/`
3. Очищен кэш `.next/`
4. Перезапущен dev сервер

**Результат:** ✅ Сайт полностью восстановлен и работает на http://localhost:3000

## ✅ Что реализовано

### 1. Поддержка многоязычного контента в БД

**Применена миграция:** `database/migrations/030_add_content_localization.sql`

Все таблицы контента теперь поддерживают:
- **Хранение оригинального языка** (en/de/ru) в поле `original_language`
- **Английский перевод** в дополнительных полях (`title_en`, `content_en`, `summary_en`)
- **Сохранение языковых предпочтений пользователя** в таблице `user_preferences`

**Таблицы с поддержкой переводов:**
- `buildings` - архитектурные объекты
- `building_reviews` - отзывы о зданиях
- `routes` - маршруты
- `news_posts` - новости
- `blog_posts` - блог
- `podcasts` - подкасты

### 2. Компоненты для работы с переводами

**Созданы компоненты:**
- `src/components/translation/TranslationFields.tsx` - универсальный компонент для добавления/редактирования переводов
- Примеры использования в `TRANSLATION_INTEGRATION_EXAMPLE.md`

### 3. Установлены зависимости

```json
{
  "next-intl": "^3.27.2"
}
```

## ❌ Что НЕ реализовано (откачено из-за архитектурных ограничений)

### 1. Локализованные URL-пути

**Изначальный план:** URL с префиксами локалей
- `/en/news`, `/de/buildings`, `/ru/profile`
- Автоматическое определение языка браузера

**Почему не реализовано:**
- Текущая архитектура приложения имеет **50+ страниц в корне `app/`**
- next-intl с App Router требует, чтобы **ВСЕ страницы** были внутри `app/[locale]/`
- Миграция всех страниц потребовала бы масштабного рефакторинга

**Результат:** URL остаются прежними (`/news`, `/buildings`, `/profile`)

### 2. Интеграция UI-переводов через next-intl

**Откачены файлы:**
- `src/app/[locale]/layout.tsx` и `page.tsx`
- `src/i18n/routing.ts`, `config.ts`, `request.ts`
- Файлы переводов `src/i18n/locales/*/common.json`

## ⚠️ Проблемы, с которыми столкнулись при реализации

### 1. Архитектурный конфликт с next-intl

**Проблема:** Приложение имеет **50+ страниц в корне `app/`** (admin, auth, blog, buildings, news, podcasts, profile, routes, settings и др.), но next-intl с App Router требует, чтобы **ВСЕ страницы** находились внутри `app/[locale]/`.

**Симптомы:**
- Постоянные 404 ошибки на всех маршрутах
- `GET /en 404` в логах сервера
- Компиляция проходила успешно, но страницы не отображались

**Попытки исправления (не помогли):**
1. Исправление типов params для Next.js 15 (`Promise<{ locale: string }>`)
2. Изменение структуры корневого layout
3. Создание файла `i18n/routing.ts` с `defineRouting`
4. Обновление middleware для интеграции next-intl
5. Добавление валидации локали через `hasLocale`
6. Множественная очистка `.next` кэша
7. Изменение `localePrefix` с `'always'` на `'as-needed'`
8. Создание тестовых страниц

**Результат:** Все попытки не решили проблему 404, так как корневая причина - в несовместимости текущей структуры файлов с требованиями next-intl.

### 2. Ошибка middleware: defaultLocale is not defined

**Ошибка:**
```
ReferenceError: defaultLocale is not defined
  at middleware (src\middleware.ts:27:48)
```

**Причина:** Отсутствовал импорт `defaultLocale` из `./i18n/config`

**Решение:** Добавлен импорт, но это не решило основную проблему с роутингом.

### 3. Ошибка next-intl context в Client Components

**Ошибка:**
```
No intl context found. Have you configured the provider?
```

**Причина:** Компонент `LanguageSelectionModal` пытался использовать `useRouter()` из next-intl до настройки `NextIntlClientProvider`.

**Статус:** Вторичная проблема, не была решена из-за отката всех изменений.

### 4. Неполный откат после первого восстановления

**Проблема:** Директория `src/app/[locale]/` не была удалена при первом откате, что вызвало повторную поломку сайта.

**Ошибка:**
```
Module not found: Can't resolve '@/i18n/routing'
GET / 500 in 728ms
```

**Решение:** Полное удаление `app/[locale]/` и очистка кэша.

## 📋 Текущая функциональность

### Поддержка контента

✅ **Работает:**
- Хранение контента на оригинальном языке (en/de/ru)
- Хранение английских переводов
- Компонент `TranslationFields` для добавления переводов
- RLS политики для `user_preferences`

### Интерфейс

❌ **Не реализовано:**
- Переключатель языка в UI
- Перевод статических текстов интерфейса
- Модальное окно выбора языка при первом посещении

## 🔄 Рекомендации для будущей реализации UI-переводов

### Сравнение вариантов

| Критерий | Вариант 1: Упрощённая интеграция | Вариант 2: Полная миграция | Вариант 3: Своё решение |
|----------|----------------------------------|----------------------------|-------------------------|
| **Сложность** | ⭐ Низкая | ⭐⭐⭐⭐⭐ Очень высокая | ⭐⭐⭐ Средняя |
| **Время** | 2-4 часа | 12-20 часов | 6-10 часов |
| **Риск** | ✅ Минимальный | ❌ Высокий | ⚠️ Средний |
| **Локализованные URL** | ❌ Нет (/news) | ✅ Да (/en/news) | ❌ Нет |
| **Поддержка** | ✅ next-intl | ✅ next-intl | ⚠️ Своя |
| **SEO** | ⚠️ Средний | ✅ Отличный | ⚠️ Средний |

### Вариант 1: Упрощённая интеграция next-intl (⭐ РЕКОМЕНДУЕТСЯ)

Использовать **только хуки next-intl** без изменения структуры роутинга:

1. **Настроить next-intl без locale routing:**
```typescript
// next.config.ts
const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');
```

2. **Создать провайдер в корневом layout:**
```typescript
// app/layout.tsx
import { NextIntlClientProvider } from 'next-intl';
import { getLocale, getMessages } from 'next-intl/server';

export default async function RootLayout({ children }) {
  const locale = await getLocale();
  const messages = await getMessages();

  return (
    <html lang={locale}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

3. **Использовать хуки в компонентах:**
```typescript
import { useTranslations } from 'next-intl';

function MyComponent() {
  const t = useTranslations('common');
  return <h1>{t('welcome')}</h1>;
}
```

**Плюсы:**
- ✅ Не требует изменения структуры папок
- ✅ Работает с существующими URL
- ✅ Простая интеграция

**Минусы:**
- ❌ URL не локализованы (остаются `/news` вместо `/en/news`)
- ❌ Нужно вручную управлять локалью через localStorage/cookies
- ❌ Хуже для SEO (нет отдельных URL для разных языков)

**Оценка:**
- Время реализации: 2-4 часа
- Риск: Минимальный
- Подходит для: Быстрого запуска многоязычности без кардинальных изменений

### Вариант 2: Полная миграция на [locale] routing

Переместить ВСЕ страницы в `app/[locale]/`:

**Требуется:**
- Переместить 50+ файлов страниц из `app/` в `app/[locale]/`
- Обновить все импорты и внутренние ссылки
- Протестировать каждый роут (admin, auth, blog, buildings, news, podcasts, profile, routes, settings)
- Обновить middleware для правильной работы с локалями
- Создать редиректы для старых URL

**Плюсы:**
- ✅ Локализованные URL (`/en/news`, `/de/buildings`, `/ru/profile`)
- ✅ Лучше для SEO (Google индексирует каждую языковую версию отдельно)
- ✅ Автоматическое определение языка браузера
- ✅ Полная поддержка всех возможностей next-intl

**Минусы:**
- ❌ Очень трудоёмко (50+ файлов)
- ❌ Высокий риск ошибок
- ❌ Может сломать существующие закладки/ссылки пользователей
- ❌ Требует обновления всех внешних ссылок на сайт

**Оценка:**
- Время реализации: 12-20 часов
- Риск: Очень высокий
- Подходит для: Крупного рефакторинга с запланированным downtime

### Вариант 3: Собственное лёгкое решение на React Context

Создать простую систему переводов без next-intl:

**Архитектура:**
```typescript
// src/i18n/translations.ts
export const translations = {
  en: {
    common: {
      welcome: 'Welcome',
      search: 'Search...'
    },
    nav: {
      map: 'Map',
      news: 'News',
      blog: 'Blog'
    }
  },
  de: { /* ... */ },
  ru: { /* ... */ }
}

// src/i18n/LocaleProvider.tsx
'use client'
import { createContext, useContext, useState, useEffect } from 'react'

type Locale = 'en' | 'de' | 'ru'

const LocaleContext = createContext<{
  locale: Locale
  setLocale: (locale: Locale) => void
  t: (key: string) => string
}>({ locale: 'en', setLocale: () => {}, t: () => '' })

export function LocaleProvider({ children }: { children: React.ReactNode }) {
  const [locale, setLocaleState] = useState<Locale>('en')

  useEffect(() => {
    const saved = localStorage.getItem('locale') as Locale
    if (saved) setLocaleState(saved)
  }, [])

  const setLocale = (newLocale: Locale) => {
    setLocaleState(newLocale)
    localStorage.setItem('locale', newLocale)
  }

  const t = (key: string) => {
    const keys = key.split('.')
    let value: any = translations[locale]
    for (const k of keys) {
      value = value?.[k]
    }
    return value || key
  }

  return (
    <LocaleContext.Provider value={{ locale, setLocale, t }}>
      {children}
    </LocaleContext.Provider>
  )
}

export const useLocale = () => useContext(LocaleContext)
```

**Использование:**
```typescript
// В компонентах
import { useLocale } from '@/i18n/LocaleProvider'

function MyComponent() {
  const { t, locale, setLocale } = useLocale()

  return (
    <>
      <h1>{t('common.welcome')}</h1>
      <button onClick={() => setLocale('de')}>Deutsch</button>
    </>
  )
}
```

**Плюсы:**
- ✅ Полный контроль над реализацией
- ✅ Минимальная зависимость от библиотек
- ✅ Легко адаптировать под текущую архитектуру
- ✅ Быстрая разработка (4-6 часов)

**Минусы:**
- ❌ URL не локализованы
- ❌ Нет встроенной поддержки плюралов, дат, чисел
- ❌ Нужно вручную поддерживать и расширять
- ❌ Меньше возможностей, чем у next-intl

**Оценка:**
- Время реализации: 6-10 часов
- Риск: Средний
- Подходит для: Проектов с особыми требованиями к i18n

## 📦 Полезные созданные файлы

Сохранены для будущего использования:
- `database/migrations/030_add_content_localization.sql` - миграция БД
- `src/components/translation/TranslationFields.tsx` - компонент переводов
- `TRANSLATION_INTEGRATION_EXAMPLE.md` - примеры интеграции
- `src/components/LanguageSwitcher.tsx` - готовый компонент переключателя языка
- `src/components/LanguageSelectionModal.tsx` - модальное окно выбора языка

## 🎯 Следующие шаги (опционально)

1. **Интегрировать TranslationFields в админ-панель:**
   - Добавить в форму создания/редактирования новостей (`/admin/news/create`, `/admin/news/[id]/edit`)
   - Добавить в форму создания зданий
   - Добавить в форму создания маршрутов

2. **Реализовать простой UI-перевод (Вариант 1):**
   - Настроить next-intl без routing
   - Создать файлы переводов для UI
   - Добавить переключатель языка в Header

3. **Создать API для получения контента на выбранном языке:**
```typescript
// Пример: получение новости с переводом
const locale = getLocaleFromCookie(); // 'en' | 'de' | 'ru'
const news = await getNewsWithTranslation(newsId, locale);
// Вернёт title_en если locale='en', иначе title
```

## 💡 Итоговые рекомендации

### ✅ ПРИНЯТО: Отложенная локализация

**Решение:**
1. **v0.1.4 (текущая):** Оставить русский язык, не трогать стабильную версию
2. **v0.1.5 (pre-launch):** Перевести весь интерфейс и контент на английский
3. **v1.0+ (post-launch):** Реализовать полную локализацию (Вариант 2)

**Преимущества:**
- ✅ Запуск стабильного продукта без риска
- ✅ Английский язык как primary для международной аудитории
- ✅ БД и компоненты уже готовы к переводам
- ✅ Возможность протестировать продукт перед масштабной локализацией

---

## 📦 Что готово к использованию

### ✅ Можно использовать сейчас:
1. **База данных:**
   - Миграция `030_add_content_localization.sql` применена
   - Все таблицы поддерживают `original_language` и `*_en` поля
   - RLS политики для `user_preferences`

2. **Компоненты:**
   - `src/components/translation/TranslationFields.tsx` - готов к интеграции
   - `src/components/LanguageSwitcher.tsx` - можно использовать после настройки next-intl
   - `src/components/LanguageSelectionModal.tsx` - готов к использованию

3. **Документация:**
   - `TRANSLATION_INTEGRATION_EXAMPLE.md` - примеры интеграции компонентов
   - `database/migrations/APPLY_030_MIGRATION.md` - информация о миграции

### ⏸️ Отложено до v1.0+:
1. **next-intl с [locale] routing:**
   - Локализованные URL (`/en/news`, `/de/buildings`)
   - Автоопределение языка браузера
   - Middleware для locale routing

2. **UI-переводы:**
   - Файлы переводов `src/i18n/locales/*/common.json`
   - `NextIntlClientProvider` в layout
   - Хуки `useTranslations()`

---

## 🎯 План миграции на английский (v0.1.5)

### Шаг 1: Перевод UI (2-3 дня)
- [ ] Все тексты в компонентах
- [ ] Сообщения об ошибках и валидация
- [ ] Email уведомления
- [ ] SEO метаданные

### Шаг 2: Перевод контента БД (1-2 дня)
- [ ] Описания зданий
- [ ] Новости и блог-посты
- [ ] Названия и описания маршрутов
- [ ] Обновить существующие записи

### Шаг 3: Тестирование (1 день)
- [ ] Проверить все страницы
- [ ] Проверить форматирование дат/чисел
- [ ] Проверить email уведомления
- [ ] Проверить SEO метаданные

---

## 🚀 План полной локализации (v1.0+)

### Вариант 2: Полная миграция на [locale] routing (РЕКОМЕНДУЕТСЯ для v1.0+)

**Этапы реализации:**
1. **Подготовка (2-3 часа):**
   - Создать детальный план миграции всех 50+ страниц
   - Настроить next-intl с `[locale]` routing
   - Подготовить файлы переводов для всех языков

2. **Миграция (8-12 часов):**
   - Переместить все страницы из `app/` в `app/[locale]/`
   - Обновить все импорты и внутренние ссылки
   - Настроить middleware для locale routing
   - Создать редиректы со старых URL

3. **Тестирование (2-4 часа):**
   - Протестировать каждый маршрут для всех локалей
   - Проверить SEO для разных языков
   - Убедиться в работе всех форм и модалов

4. **Запуск (1-2 часа):**
   - Deploy на production
   - Мониторинг ошибок через Sentry
   - Обновление sitemap.xml для всех языков

**Итого:** 12-20 часов

---

## ❌ Варианты, которые НЕ рекомендуются

### Вариант 1: Упрощённая интеграция next-intl
**Причина отказа:** Не даёт локализованных URL, что плохо для SEO и UX.
**Когда использовать:** Никогда. Лучше отложить полную локализацию до v1.0+.

### Вариант 3: Своё решение на React Context
**Причина отказа:** Нет локализованных URL, требует ручной поддержки, нет плюралов/дат.
**Когда использовать:** Только если есть специфические требования, которые next-intl не покрывает.

## 📚 Документация

- **Миграция БД:** `database/migrations/APPLY_030_MIGRATION.md`
- **Примеры использования:** `TRANSLATION_INTEGRATION_EXAMPLE.md`
- **Исходный план:** `I18N_IMPLEMENTATION_PLAN.md`
- **Этот статус:** `I18N_IMPLEMENTATION_STATUS.md`
