# ПРОГРЕСС ПЕРЕРАБОТКИ РАЗДЕЛА БЛОГА

**Дата начала:** 14 ноября 2025
**Статус:** В процессе - Фаза 2/12
**Прогресс:** ████░░░░░░░░░░░░░░░░ 20%

---

## 🎯 ЦЕЛЬ ПРОЕКТА

Переработка раздела блога с внедрением модульного конструктора блоков (по аналогии с /news), интеграцией с объектами архитектуры и возможностью построения маршрутов.

### Ключевые особенности:
- ✅ Модульный drag-and-drop конструктор (как в /news)
- ✅ 6 типов блоков контента
- ✅ Карточки объектов с интеграцией в базу buildings
- ✅ Интерактивная карта упомянутых объектов
- ✅ Построение маршрутов из объектов блога
- ✅ Социальные функции (лайки, коллекции, шаринг)
- ✅ Зелёные акцентные кнопки

---

## ✅ ВЫПОЛНЕНО

### Фаза 1: База данных и типы (100% завершено)

#### 1.1 Миграция БД ✅
**Файл:** `database/migrations/020_create_blog_content_blocks.sql`

**Создано:**
- Таблица `blog_content_blocks` со следующими полями:
  - `id` (UUID)
  - `blog_post_id` (FK → blog_posts)
  - `order_index` (INTEGER)
  - `block_type` (VARCHAR) - 6 типов
  - `content` (TEXT)
  - `building_id` (FK → buildings, nullable)
  - `images_data` (JSONB)
  - `block_settings` (JSONB)
  - `created_at`, `updated_at`

**Типы блоков:**
1. `text` - Текстовый блок
2. `text_image_right` - Текст + изображение справа
3. `image_text_left` - Изображение слева + текст
4. `full_width_image` - Полноразмерное изображение
5. `gallery` - Галерея изображений
6. `building_card` - Карточка объекта

**Индексы:**
- `idx_blog_content_blocks_post_order` на (blog_post_id, order_index)
- `idx_blog_content_blocks_type` на block_type
- `idx_blog_content_blocks_building` на building_id
- `idx_blog_content_blocks_images` GIN на images_data

**RLS политики:**
- Публичный доступ к блокам опубликованных постов
- Авторы видят свои блоки
- Админы/модераторы видят все блоки
- CRUD права для авторов и админов

**Обновления:**
- Добавлено поле `editor_version` в `blog_posts`:
  - `'rich_text'` - старый редактор
  - `'blocks'` - модульный конструктор

#### 1.2 TypeScript типы ✅
**Файл:** `src/types/blog.ts`

**Добавлено:**
- `BlogContentBlockType` - union type для 6 типов блоков
- `ImageData` - интерфейс для данных изображения
- `BlogContentBlock` - основной интерфейс блока
- `CreateBlogContentBlock` - интерфейс для создания блока
- Settings интерфейсы для каждого типа:
  - `TextBlockSettings`
  - `TextImageRightBlockSettings`
  - `ImageTextLeftBlockSettings`
  - `FullWidthImageBlockSettings`
  - `GalleryBlockSettings`
  - `BuildingCardBlockSettings` (специальный для объектов)

**Обновлено:**
- `BlogPost` интерфейс:
  - Добавлено `editor_version?: 'rich_text' | 'blocks'`
  - Добавлено `content_blocks?: BlogContentBlock[]`

#### 1.3 Утилиты ✅
**Файл:** `src/utils/blogBlocks.ts`

**Реализовано:**

**Фабрика создания блоков:**
- `createTextBlock()`
- `createTextImageRightBlock()`
- `createImageTextLeftBlock()`
- `createFullWidthImageBlock()`
- `createGalleryBlock()`
- `createBuildingCardBlock()`
- `createBlockByType()` - универсальная фабрика

**Манипуляции с блоками:**
- `addBlock()` - добавить блок в конец
- `removeBlock()` - удалить и пересчитать индексы
- `moveBlock()` - переместить блок
- `reorderBlocks()` - пересчитать все индексы
- `duplicateBlock()` - дублировать блок

**Обновление данных:**
- `updateBlockContent()` - обновить текст
- `updateBlockBuilding()` - обновить building_id
- `updateBlockImages()` - обновить изображения
- `updateBlockSettings()` - обновить настройки

**Утилиты:**
- `getBlockTypeName()` - человекочитаемое название
- `getBlockTypeIcon()` - эмодзи иконка
- `getBlockTypeDescription()` - описание блока
- `getTotalImagesCount()` - подсчет изображений
- `getTotalTextLength()` - подсчет символов
- `extractBuildingIds()` - извлечь все building_id
- `getBlockTypeStats()` - статистика по типам
- `validateBlock()` - валидация одного блока
- `validateAllBlocks()` - валидация всех блоков
- `hasValidationErrors()` - проверка наличия ошибок

**Конвертация:**
- `convertLegacyContentToBlocks()` - миграция старого контента

---

## 🚧 В ПРОЦЕССЕ

### Фаза 2: Система блоков - базовые блоки (20% завершено)

**Что нужно создать:**

#### Компоненты отображения (для чтения блога):
📁 `src/components/blog/blocks/`

- ⬜ `TextBlock.tsx` - отображение текстового блока
- ⬜ `TextImageRightBlock.tsx` - текст + картинка справа
- ⬜ `ImageTextLeftBlock.tsx` - картинка слева + текст
- ⬜ `FullWidthImageBlock.tsx` - полноразмерное изображение
- ⬜ `GalleryBlock.tsx` - галерея изображений
- ⬜ `BuildingCardBlock.tsx` - карточка объекта ⭐ (ключевой компонент)

#### Компоненты редактирования (для создания/редактирования):
📁 `src/components/blog/blocks/`

- ⬜ `TextBlockEditor.tsx` - редактор текста
- ⬜ `TextImageRightBlockEditor.tsx` - редактор текст+картинка
- ⬜ `ImageTextLeftBlockEditor.tsx` - редактор картинка+текст
- ⬜ `FullWidthImageBlockEditor.tsx` - редактор полноразмерного изображения
- ⬜ `GalleryBlockEditor.tsx` - редактор галереи
- ⬜ `BuildingCardBlockEditor.tsx` - редактор карточки объекта ⭐

#### Вспомогательные компоненты:
- ⬜ `BlockEditorWrapper.tsx` - обёртка с drag handle и кнопками

---

## 📅 ПЛАН НА БУДУЩЕЕ

### Фаза 3: Блок BuildingCard (приоритет)

**Компоненты:**
- ⬜ `BuildingCardBlock.tsx` - отображение карточки
- ⬜ `BuildingCardBlockEditor.tsx` - редактор с выбором объекта

**Функционал BuildingCardBlock:**
- Красивая карточка с фото объекта
- Название, архитектор, год, стиль
- Зелёная кнопка "Показать на карте"
- Зелёная кнопка "Добавить в маршрут"
- Horizontal/Vertical layout

**Функционал BuildingCardBlockEditor:**
- Кнопка "Выбрать объект из базы" → BuildingSelector
- Кнопка "Создать новый объект" → QuickBuildingCreator
- Превью выбранного объекта
- Настройки отображения (галочки для полей)

### Фаза 4: Основные компоненты редактора

#### 4.1 ContentBlockEditor ⭐ (КРИТИЧНЫЙ)
**Файл:** `src/components/blog/ContentBlockEditor.tsx`

**Функционал:**
- Интеграция @dnd-kit для drag-and-drop
- Список блоков с возможностью перестановки
- Рендеринг редакторов блоков
- Управление состоянием блоков
- Передача изменений в родительский компонент

**Технологии:**
- `@dnd-kit/core` - основа DnD
- `@dnd-kit/sortable` - сортировка
- `useSensors` для мыши и клавиатуры

#### 4.2 ContentBlockRenderer
**Файл:** `src/components/blog/ContentBlockRenderer.tsx`

**Функционал:**
- Switch по типу блока
- Рендеринг соответствующего компонента
- Передача данных блока

#### 4.3 BlockToolbar
**Файл:** `src/components/blog/BlockToolbar.tsx`

**Функционал:**
- Sticky панель сверху
- Кнопка "+ Добавить блок"
- Dropdown с типами блоков
- Иконки и описания
- Зелёный акцент для кнопок

### Фаза 5: Обновление страниц блога

#### 5.1 Страница создания
**Файл:** `src/app/blog/create/page.tsx`

**Изменения:**
- Убрать старый BlogEditor
- Добавить ContentBlockEditor
- Форма метаданных (title, excerpt, featured_image)
- Сохранение блоков в БД
- Автосохранение

#### 5.2 Страница редактирования
**Файл:** `src/app/blog/[slug]/edit/page.tsx`

**Изменения:**
- Загрузка существующих блоков
- ContentBlockEditor с данными
- Обновление блоков

#### 5.3 Страница чтения
**Файл:** `src/app/blog/[slug]/page.tsx`

**Изменения:**
- Загрузка блоков с buildings (join)
- ContentBlockRenderer для отображения
- Стилизация под 34travel.me

### Фаза 6: Интерактивная карта блога

#### 6.1 Компонент карты
**Файл:** `src/components/blog/BlogArticleMap.tsx`

**Функционал:**
- Отображение всех объектов из building_card блоков
- Использование EnhancedMap или LeafletMap
- Клик по маркеру → BuildingModal
- Кнопка "Открыть на /map?building=ID"
- Collapsible блок

**Технологии:**
- Dynamic import с ssr: false
- Leaflet для карты
- Clustering для множества объектов

#### 6.2 Размещение
- После контента блога
- Перед похожими постами
- Показывать только если есть объекты

### Фаза 7: Построение маршрутов из блога

#### 7.1 BlogRouteBuilder
**Файл:** `src/components/blog/BlogRouteBuilder.tsx`

**Функционал:**
- Зелёная кнопка "Составить маршрут"
- Модальное окно со списком объектов
- Чекбоксы для выбора
- Сортировка: по порядку / по расстоянию
- Интеграция с RouteCreator

**Процесс:**
1. Извлечь все building_id из блоков
2. Загрузить данные объектов
3. Показать список с превью
4. Выбрать объекты
5. Построить маршрут через Mapbox
6. Сохранить с `created_from_blog_post_id`

#### 7.2 Отображение маршрутов
**Компонент:** Список созданных маршрутов

**Функционал:**
- Показать маршруты, созданные из этого блога
- Кнопка "Посмотреть" → RouteViewerModal
- Счётчик создано маршрутов

### Фаза 8: Социальные функции

#### 8.1 Компонент SocialActions
**Файл:** `src/components/blog/SocialActions.tsx`

**Кнопки (все зелёные):**
- 👍 Нравится - с анимацией и счётчиком
- 📚 В коллекцию - закладка для сохранения
- 🔗 Поделиться - dropdown с соцсетями

**Интеграция:**
- Использует `blog_post_reactions` таблицу
- Real-time обновление счётчиков
- Авторизация required

#### 8.2 Шаринг
**Функционал:**
- Копировать ссылку
- Twitter, Facebook, VK, Telegram
- Отслеживание в БД

#### 8.3 Коллекции
**Функционал:**
- Сохранение в профиль пользователя
- Страница "Моя коллекция" в профиле
- Счётчик сохранений

### Фаза 9: Создание объектов из редактора

#### 9.1 QuickBuildingCreator
**Файл:** `src/components/blog/QuickBuildingCreator.tsx`

**Функционал:**
- Мини-форма создания здания
- Поля: название, категория, адрес, координаты, фото
- LocationPicker для выбора на карте
- Валидация координат
- Сохранение с `moderation_status = 'pending'`

**Интеграция:**
- Открывается из BuildingCardBlockEditor
- После создания → автоматический выбор
- Объект добавляется в базу buildings

#### 9.2 Переиспользование
- Использовать форму из /map
- Модальное окно
- Упрощённая версия

### Фаза 10: Миграция существующих блогов

#### 10.1 Скрипт миграции
**Файл:** `scripts/migrateBlogsToBlocks.ts`

**Функционал:**
- Конвертация rich text → блоки
- Парсинг HTML:
  - Параграфы → text блоки
  - Изображения → image блоки
  - Building mentions → building_card блоки
- Обновление `editor_version = 'blocks'`

#### 10.2 Безопасность
- Backup таблица с оригиналами
- Возможность отката
- Логирование процесса

### Фаза 11: Оптимизация и полировка

#### 11.1 SEO
- Генерация excerpt из первого текстового блока
- Извлечение featured_image из первого изображения
- Open Graph теги
- Structured data (JSON-LD)

#### 11.2 Производительность
- Lazy loading изображений
- Кеширование карты
- Оптимизация запросов (join)
- Image optimization (WebP, srcset)
- Bundle optimization

#### 11.3 Accessibility
- ARIA labels для drag handles
- Keyboard navigation
- Alt text обязателен
- Focus management в модалках
- Screen reader support

### Фаза 12: Тестирование

#### 12.1 Функциональное
- Создание блога с разными блоками
- Drag-and-drop перестановка
- Добавление/создание объектов
- Построение маршрутов
- Социальные функции

#### 12.2 Интеграционное
- Блог → объекты → карта
- Блог → маршруты
- Real-time подписки
- RLS права доступа

#### 12.3 UI/UX
- Мобильная версия
- Разные разрешения
- Длинные тексты
- Много изображений
- Пустые состояния

---

## 📊 СТАТИСТИКА ПРОЕКТА

### Созданные файлы (3):
1. ✅ `database/migrations/020_create_blog_content_blocks.sql` (207 строк)
2. ✅ `src/types/blog.ts` (обновлён, +139 строк)
3. ✅ `src/utils/blogBlocks.ts` (566 строк)

### Файлы к созданию (~35):
- Блоки отображения: 6 файлов
- Блоки редактирования: 6 файлов
- Основные компоненты: 3 файла
- Дополнительные компоненты: 4 файла
- Обновление страниц: 3 файла
- Скрипты и утилиты: 2 файла
- Тесты: ~10 файлов

### Оценка времени:
- ✅ Завершено: ~3 часа
- 🚧 В процессе: ~2-3 часа
- ⬜ Осталось: ~30-40 часов

**Общая оценка:** 35-46 часов (3-5 рабочих дней)

---

## 🎨 ДИЗАЙН И СТИЛИЗАЦИЯ

### Цветовая схема блога:
- **Акцентный цвет:** Зелёный (`#10B981` или из tailwind `green-500`)
- **Кнопки:** Зелёные с hover эффектами
- **Фон:** Светлый, минималистичный
- **Текст:** Читабельный, как на 34travel.me

### Компоненты с зелёными кнопками:
1. BuildingCardBlock:
   - "Показать на карте" (зелёная)
   - "Добавить в маршрут" (зелёная)

2. BlogRouteBuilder:
   - "Составить маршрут" (зелёная, большая)

3. SocialActions:
   - Все кнопки с зелёным акцентом

4. BlockToolbar:
   - "+ Добавить блок" (зелёная)

---

## 🔧 ТЕХНОЛОГИИ И БИБЛИОТЕКИ

### Используемые:
- ✅ Next.js 15 (App Router)
- ✅ React 19
- ✅ TypeScript
- ✅ Supabase (БД, Auth, Storage)
- ✅ Tailwind CSS
- ✅ @dnd-kit/core, @dnd-kit/sortable (drag-and-drop)
- ✅ Leaflet (карты)
- ✅ Mapbox (routing)

### Паттерны:
- ✅ Factory pattern для создания блоков
- ✅ Wrapper pattern для общего UI
- ✅ Sortable hook pattern для DnD
- ✅ Validation pattern для форм
- ✅ Supabase factory client (не singleton!)

---

## ⚠️ РИСКИ И ОГРАНИЧЕНИЯ

### Технические риски:
1. **Конфликт DnD с contentEditable**
   - Митигация: Отключать drag во время редактирования текста

2. **Медленная загрузка карты**
   - Митигация: Clustering, lazy loading, кеширование

3. **Сложность миграции**
   - Митигация: Поддержка обоих форматов, бэкапы

4. **Потеря данных при автосохранении**
   - Митигация: Debounce, проверка успешности, localStorage

### Известные ограничения:
- Максимум 50 блоков в одном посте (производительность)
- Размер изображений ограничен 10MB
- Building_card требует существующий объект или создание нового

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ ЗАМЕТКИ

### Интеграция с существующими системами:

**Buildings:**
- Building_card блоки используют существующую таблицу
- Автоматическое добавление в `blog_post_buildings`
- Отображение на общей карте `/map`

**Routes:**
- Маршруты создаются через существующий RouteCreator
- Сохраняются в таблицу `routes`
- Поле `created_from_blog_post_id` для связи

**Auth:**
- Используется `useAuth` hook
- RLS на всех операциях
- Только авторы редактируют свои посты

### Соответствие спецификации BLOG_REDESIGN.md:

✅ Модульный конструктор как в /news
✅ 6 типов блоков (включая все из спецификации)
✅ Карточки объектов с автодобавлением в базу
✅ Интерактивная карта объектов
✅ Кнопка "Составить маршрут" с галочками
✅ Зелёные кнопки в блоге
✅ Создание объектов из редактора
✅ Drag-and-drop блоков
✅ Стиль 34travel.me
✅ Социальные функции

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

**Немедленные действия:**
1. Создать базовые блоки отображения (TextBlock, TextImageRightBlock, etc.)
2. Создать редакторы блоков
3. Создать FullWidthImageBlock
4. Создать BuildingCardBlock (приоритет)
5. Создать ContentBlockEditor с @dnd-kit
6. Создать ContentBlockRenderer
7. Создать BlockToolbar

**После основных компонентов:**
8. Обновить страницы блога
9. Добавить карту и маршруты
10. Реализовать социальные функции
11. Тестирование

---

**Последнее обновление:** 14 ноября 2025
**Автор:** Claude Code
**Версия документа:** 1.0
